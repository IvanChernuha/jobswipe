# JobSwipe — Local dev (plain HTTP on :80)
:80 {
    handle /api/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }

    encode gzip

    log {
        output stdout
        format json
    }
}
